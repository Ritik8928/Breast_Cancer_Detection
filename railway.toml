[build]
  builder = "nixpacks"

[build.nixpacks]
  plan = "cd backend && pip install -r requirements.txt"

[deploy]
  startCommand = "cd backend && gunicorn app:app --bind 0.0.0.0:$PORT"

[deploy.env]
  PYTHON_VERSION = "3.11"
  FLASK_APP = "app.py"